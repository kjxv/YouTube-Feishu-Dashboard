# 开发进度与断点

> 新会话接手顺序：先读本文件，再读根目录 `project_state.json`，最后运行 `git status --short --branch`。从“下一步”继续，不需要重新梳理历史对话。

## 当前阶段

- 2026-09-10 已完成最新长视频自动发现与生命周期维护：每轮从频道上传列表筛选最近28天内已公开长视频，最新一期写“当前追踪视频=是”，其余窗口内视频保持“追踪中/否”，超过窗口后写“追踪结束”
- 已修复 Windows/VPS 或空数据库可能为同一视频建立两条主记录的问题：三张视频表写入均按远程业务唯一键认领；视频主表每轮还会合并同视频重复行并修复本地绑定
- 飞书快照表的“当前追踪视频”继续使用按视频ID查找引用，不批量改写历史快照；主表去重且状态收敛后，引用结果会从两个“是”恢复为单一状态
- `tracking_video_ids` 暂时保留为兼容和应急补充，未来新视频无需人工逐期添加；自动发现会在视频公开后的下一次整点运行中纳入
- 当前源码验收：全量174项测试通过、Ruff通过、严格Mypy（80个源码文件）通过、pip依赖检查通过；未请求真实YouTube/飞书、未执行Git提交或推送
- 当前下一步：用户推送 Git，在唯一运行的 VPS 上执行一键更新；下一轮真实任务会自动清理现有重复主记录并校正新旧视频状态
- 2026-09-07 已按用户确认口径补齐“最近一次调度结果”和“本轮飞书写入记录数”：摘要只判断最新视频 Data API 是否实际采集，不使用 Analytics/Reporting 延迟时间反推任务状态
- 摘要明确区分“没有业务任务到期”“Data API 本轮无需采集”“Data API 已采集且飞书已更新N条”“业务数据无变化”“任务失败或跳过”
- 运行电脑仍需先更新 Git 并执行第14个入口；该入口才会在线创建或补齐“系统运行状态”表，本地完成创建能力不等于已修改线上飞书
- 2026-09-07 已定位 Windows 早上 08:00 未更新：任务计划程序结果 `0x41301` 表示进程仍在运行，不是执行成功；由于任务设置为禁止重叠，09:00 唤醒会被跳过
- 已完成定时任务可观测性：新增飞书单行“系统运行状态”表、Windows 第16个完整诊断入口、本地 `scheduler status`、进程/日志诊断以及开始/结束/退出码/耗时日志
- 每次 `scheduler tick` 都会更新飞书状态，即使当轮没有业务任务到期；长时间停留在“运行中”即可判断启动后卡住，业务数据不再承担系统心跳用途
- Google API 公共传输已增加 60 秒明确网络超时；Windows 单轮任务上限由2小时降至45分钟，需在运行电脑重新“安装或更新”任务后生效
- 运行电脑下一步：更新 Git 后运行第14个入口，确认自动创建“系统运行状态”表；再运行第10个入口选择“安装或更新”，随后可运行第16个入口并观察下一整点状态
- 当前源码验收：全量172项测试通过、Ruff通过、严格Mypy（80个源码文件）通过、两个 PowerShell 脚本语法解析通过；未请求 YouTube/飞书、未写外部数据、未执行 Git 操作
- 2026-09-07 已完成 Linux VPS 7×24 小时运行材料：新增频道模块只读/手动入口、升级并验收入口、SQLite 在线备份、统一 systemd timer 安装与管理脚本
- VPS 当前正式推荐“单台 Linux VPS + SQLite + systemd”，timer 每个整点唤醒统一 `scheduler tick`；业务模块仍按飞书配置和数据库到期状态判断是否真正采集
- 迁移必须先暂停 Windows 生产任务，再安全复制 `.env`、两个 YouTube OAuth 文件和当前 SQLite 数据库；Windows 与 VPS 禁止同时写入
- systemd 使用 `OnCalendar=hourly` 和 `AccuracySec=1s` 对齐每个整点；oneshot 服务与统一数据库任务锁负责避免重叠，服务同时采用独立运行用户、`UMask=0077`、`NoNewPrivileges` 和 `PrivateTmp`
- 当前源码验收：全量157项测试通过、Ruff通过、严格Mypy（79个源码文件）通过；Linux shell/systemd 的真实安装需要在 VPS 上按部署文档做最终验收
- 当前下一步：用户提交并推送本次改动，在 VPS 按 `00_项目文档/VPS部署说明.md` 完成单机迁移和 2～3 天观察；不在 Windows 与 VPS 同时开启定时任务
- 2026-09-06 已接入频道“过去28日估算收入”：使用 Analytics API `estimatedRevenue`、`currency=USD` 和太平洋自然日窗口，按天求和后写入最新的频道采样快照；实际 API 数据截止日同时写入统一 Analytics 时间字段
- 收入采集仅在共享映射启用标准字段 `ANALYTICS_EST_REVENUE` 时执行；旧的 `ANALYTICS_EST_AD_REVENUE` 不是同一指标，不能代替 Studio 概览中的“估算收入”
- 运行电脑更新后，先执行第14个入口完成安装、目录同步和只读验收，再执行第12个入口验证一次真实收入写入；确认无误后由第10个入口维护统一定时任务。无需重新执行第15个入口，频道开关已经启用
- 当前源码验收：全量153项测试通过、Ruff通过、严格Mypy（77个源码文件）通过、pip check通过；未执行Git提交或推送
- 2026-09-06 运行电脑已完成第14、15个入口：频道开关已启用，首次真实同步成功；67个长视频、67条每日快照、335条视频 Analytics、1条频道快照和5条频道 Analytics 已同步，475条远程记录完成安全认领
- 首次同步截图确认三个48小时字段均为空；原因是运行电脑本地没有这些旧视频在发布后2880分钟附近（默认±90分钟）的真实 Data API 快照，认领飞书记录不会反向导入旧快照，也不会用当前累计值伪造历史节点
- 下一步是在运行电脑打开 `10.自动定时任务管理.bat`，选择“安装或更新”，再查看状态；频道每日任务和最新视频实时追踪共用这一个每小时唤醒的统一计划任务
- 同步结果现已增加 `forty_eight_hour_samples_found` 和 `forty_eight_hour_samples_missing`，以后可直接从日志判断48小时样本覆盖情况；第14、15个入口末尾提示改为ASCII，避免Windows命令解释器把中文提示误当命令
- 当前自动化基线：153项测试、Ruff和严格Mypy（77个源码文件）通过
- 2026-09-06 频道每日统计（长视频）模块已完成真实三表同步、飞书批量写入优化和换电脑安全认领能力
- 三张频道业务表真实规模：视频主表67条、视频历史469条、频道历史7条；真实回读无重复键
- 543条业务记录已由逐行请求优化为3次表级批量请求；任务日志区分记录变化数、批量请求数和认领数
- 新电脑本地绑定为空时会按业务唯一键认领飞书现有记录；同键多行立即停止，不复制或覆盖另一台电脑的SQLite数据库
- 发布后48小时播放量已接入本地代码：从真实 Data API 快照选择最接近2880分钟的一条，默认容差±90分钟，缺失时不伪造
- 48小时榜需要的播放量、实际发布后分钟数和北京时间取样时间已纳入公共字段字典与频道视频主表默认映射
- 飞书频道视频主表已在线补建3个48小时字段，共享映射由17条增至20条；只读回查确认三个字段均为“可直接使用”且频道三表安全状态为 true
- 已新增可重复执行的 `feishu enable-channel-48h-fields` 命令和 Windows 第13个入口；类型冲突、重复字段或重复映射会在业务写入前停止
- 已新增 Windows 第14个“一键升级并验收”入口，供真正运行定时任务的电脑在 Git 更新后依次完成安装、在线健康检查、48小时字段确认和频道三表只读验收
- 已新增第15个“启用并首次同步”入口和幂等远程开关命令；首次同步失败时自动回滚为关闭，避免统一计划任务反复失败
- 远程 `channel_history_enabled` 已由运行电脑第15个入口成功设为 `true`
- 当前自动化基线：153项测试、Ruff和严格Mypy（79个源码文件）通过；正式 wheel 已重新构建并安装，安装后全量测试与依赖检查通过
- 当前下一步：在实际运行电脑用第10个入口安装或更新统一计划任务并查看状态；不重复运行第15个入口
- 2026-09-04 字段可用性、时间与收益调整第 6/7 步已完成：当前源码已构建并重新安装到正式 `runtime/venv`
- 正式安装包位于 `runtime/artifacts/field_availability_step6/youtube_feishu_dashboard-0.1.0-py3-none-any.whl`，SHA256 为 `E2E17512AFD275FFC9213D2DFE5B358F9333595599A0067470CC893411A7683F`
- 从已安装的 `site-packages` 而非源码路径验收：目录版本 `2026.09.v5`、166个字段、81/82/3三态数量、预计广告收入可直接使用、12个双时区字段均正确
- 正式环境全量124项测试通过，`pip check` 无损坏依赖，`yfd --version` 为 `0.1.0`
- 构建过程中发现正式环境缺少 `build`/`hatchling`；已安装并写入开发依赖，后续环境可重复构建
- 本步骤仅联网下载公开构建工具，没有连接 YouTube 或飞书，没有写业务表，也没有执行第9/8/6批处理
- 当前下一步：第7步由用户先同步字段字典，再依次执行第9、第8、第6个入口并截图；根据结果完成最终在线验收
- 2026-09-04 字段可用性与收益接入调整第 5/7 步已完成：`ANALYTICS_EST_AD_REVENUE` 已正式接入当前视频 Analytics 查询、缓存和动态飞书写入链路
- 预计广告收入按视频统计，程序明确使用 `currency=USD`；本机 OAuth Token 已确认具有普通 Analytics 只读和收益只读权限
- 旧 Analytics 缓存缺少新收益字段时会绕过24小时缓存立即刷新；刷新失败则安全留空，不再触发“运行结果缺少已映射字段”
- 字典状态已更新为81项可直接使用、82项底层已实现待接入、3项底层未实现；预计广告收入现为“可直接使用”
- 第5步本地全量124项测试、Ruff和严格Mypy全部通过；未连接飞书或 YouTube、未安装运行环境、未执行第9/8/6批处理
- 当前下一步：第6步构建当前项目并安装到正式 `runtime/venv`，然后只做本地运行环境验收；在线第9/8/6留到第7步由用户执行
- 2026-09-04 字段可用性与时间体系调整第 4/7 步已完成：三种 API 的获取时间、数据截止时间均提供太平洋/北京双时区字段
- Data API 的截止时间明确标记为“推定”；Analytics/Reporting 的官方太平洋截止日期按自然日末转换成同一时刻的北京表示
- 新增12个时间展示字段后，本地内置字典共166项：80项可直接使用、83项底层已实现待接入、3项底层未实现
- 双时区展示字段必须映射到飞书文本列，值中保留 `-07:00`、`-08:00` 或 `+08:00` 偏移量；日期列不能同时显示两个时区
- Reporting 只有当前视频实际命中日报数据时才登记截止时间；没有新日报时保留上次真正获取时间，避免把每日检查时间误当作数据获取时间
- 第4步本地全量119项测试、Ruff和严格Mypy全部通过；未连接飞书、未安装运行环境、未执行第9/8/6批处理
- 当前下一步：第5步接入并修正预计广告收入 `ANALYTICS_EST_AD_REVENUE`，解决新增收益指标后缓存记录缺字段的问题
- 2026-09-04 Analytics/Reporting 数据接入第 7/7 步已完成：筛选后的正确映射已启用，第 9、8、6 个入口及三表在线回读验收通过
- 本次正式运行 ID 为 `7b26c0ee-a05a-4ad2-b68a-695a481196e8`：主表 1、快照 1、同期对比 3，共 5 次飞书写入
- Analytics API 已成功刷新 7 项指标，统计截止到 `2026-09-01`；回读确认互动观看量 979、观看时长 2407 分钟、平均观看 146 秒、平均观看百分比 29.16%、新增订阅 17、减少订阅 1、分享 5，飞书公式算出的净增订阅为 16
- Reporting API 的日报任务已创建，但 YouTube 尚未生成首份日报；展示次数和展示点击率暂时为空，后续等日报生成后再按 9→8→6 重跑即可
- 为防止错误写入，本轮关闭 4 条映射：主表“观看时长（小时）”（源值为分钟）、主表和快照“预计收入”（当前授权无收益只读权限）、同期表“净增订阅”（不能直接等于新增订阅）；净增订阅继续由飞书公式计算
- 当前下一步：等待 YouTube 生成首份 Reporting 日报后，按 9→8→6 再运行一次并核对展示次数/展示点击率；预计收入如以后需要，先补收益权限和安全验收
- 当前源码已重新构建并安装到 `runtime/venv`；正式 SQLite 数据库已从 `20260831_0001` 升级到 `20260903_0003`，升级前备份为 `runtime/backups/yfd_before_analytics_reporting_20260903_221927.db`
- 采集周期已确认：Data API 60 分钟；Analytics API 24 小时；Reporting API 24 小时。飞书远端已保存 Data API 的 60 分钟配置，后两项缺省时使用程序的 24 小时默认值
- 第 9 个在线只读检查通过：三表可访问，47 条现有启用映射有效，完整同步安全状态为 true；Analytics/Reporting 映射仍全部关闭，尚未提前采集或写入这些字段
- 第 8 个在线只读预览通过：当前指定视频可访问、属于授权频道、仍在 7 天窗口内；本轮只读取现有 Data API 字段，没有写本地业务数据或飞书业务表
- 正式环境全套 108 项测试、Ruff、严格 Mypy（71 个源码文件）和依赖检查全部通过；离线演练确认 60/24/24 周期且无外部请求
- 第 7 步已经完成，不再重复首次在线验收；Reporting 首份日报属于异步等待项
- 2026-09-03 手动多视频追踪改造第 6/6 步已完成，正式环境和在线三表闭环验收通过
- `runtime/venv` 已安装当前源码；飞书账号非敏感配置已补建 `tracking_video_ids`，当前测试值为 `okAkZVRx7ac`
- 第9个在线只读检查通过：手动列表1个有效ID、无重复；三表可访问，47条启用映射有效，动态计划与完整同步安全状态均为true
- 第8个在线只读预览通过：视频存在、频道归属正确、仍在7天窗口内；11个API字段全部取得，缺失与空值均为0
- 第6个正式同步成功：运行ID `b5e98a7a-4433-4f2f-955f-5e19f3d3f14d`，主表1、快照1、同期对比3，共5次飞书写入
- 飞书回读确认该视频主表1条、累计快照5条、累计同期对比15条；最新播放量1442、点赞4、评论5
- 正式环境全套78项测试通过，依赖检查无损坏；没有操作浏览器、没有安装循环调度、没有提交或推送GitHub
- 日常操作已固定：在 `tracking_video_ids` 填写1～10个ID → 第9个检查配置 → 第8个在线预览 → 第6个正式同步
- 已知非阻塞项：完整配置中心初始化器会因“标准字段中文名”现为飞书类型19而模板要求文本类型1而安全停止；本轮定向补建账号配置，没有改动用户字段类型，不影响第6/8/9入口
- 本轮6步已结束。下一步由用户先把 `tracking_video_ids` 改为真正需要追踪的长视频或多视频列表，再按9→8→6操作；其他功能另行规划
- 2026-09-02 手动多视频追踪改造第 5/6 步已完成，等待用户确认后才进入第 6 步
- 第 6 个正式同步已连接 `tracking_video_ids`：在创建 YouTube 客户端和业务写入前严格解析同一份手动列表，并交给多视频批量服务
- 第 6 个安全演练会显示待处理视频 ID 和数量；正式运行沿用逐视频状态及主表/快照/同期对比/飞书写入合计
- 第 9 个只读检查新增视频列表报告：格式、数量、重复项不合格会阻止完整三表同步；它不请求 YouTube
- 第 8 个负责在线核对视频存在性、访问权限和频道归属；第 9/8 通过后，第 6 个才正式写入
- 专项25项测试和全套78项测试通过；Ruff通过，严格Mypy检查64个源码文件通过
- 本步骤没有访问真实 YouTube 或飞书、没有写业务表、没有安装正式环境、没有提交或推送 GitHub
- 下一次只做第 6 步：安装当前源码、幂等补建线上 `tracking_video_ids` 配置，先第9/8只读验收，再按用户确认的小列表完成正式三表同步验收；完成后再次停下
- 2026-09-02 手动多视频追踪改造第 4/6 步已完成，等待用户确认后才进入第 5 步
- 第 8 个只读预览已连接 `tracking_video_ids`，不再读取频道最新上传列表；多期视频按配置顺序逐条预览
- 预览状态覆盖可同步、已过期、无法访问、频道不匹配和资源解析失败；单条失败不遮住其他条目的结果
- 每条有效视频显示 API/动态字段、飞书类型转换结果和最多5次三表预计写入；过期或错误视频预计写入为0
- 重复或意外 API 返回会列为阻止正式同步的警告；汇总显示有效、过期、失败数量和最多总写入数
- 专项20项测试和全套75项测试通过；Ruff通过，严格Mypy检查64个源码文件通过
- 本步骤没有访问真实 YouTube 或飞书、没有写业务表、没有安装正式环境；线上账号配置尚未补建 `tracking_video_ids`
- 下一次只做第 5 步：把第 6 个正式同步和第 9 个配置检查连接手动多视频配置，并补齐用户可见汇总与安全检查；完成后再次停下
- 2026-09-02 手动多视频追踪改造第 3/6 步已完成，等待用户确认后才进入第 4 步
- 正式追踪服务已改为按配置 ID 一次批量读取，不再在服务内部读取频道最新上传列表或只取第一条
- 新增整批写入前检查：缺失/不可访问视频、重复或意外 API 返回、配置频道不一致、视频不属于当前频道都会阻止整批任务
- 所有有效视频会先完成时长与动态字段解析，再统一保存本地上下文并逐视频共用现有映射写三张表；配置/解析错误不会造成前一条视频提前写入
- 过期视频只跳过自身，其他有效视频继续；返回结果按用户配置顺序列出逐视频状态并汇总三表写入次数
- 快照表和同期对比表容量在每轮任务各检查一次；两视频本地测试一次批量读取后得到主表2、快照2、同期对比6，共10次写入
- 专项 19 项测试和全套 74 项测试通过；Ruff 通过，严格 Mypy 检查 64 个源码文件通过
- 本步骤没有访问真实 YouTube 或飞书、没有写业务表、没有接入第 6/8/9 个用户入口、没有安装正式环境、没有提交或推送 GitHub
- 下一次只做第 4 步：升级第 8 个只读预览，逐个显示指定视频、追踪期限和预计写入；完成后再次停下
- 2026-09-02 手动多视频追踪改造第 2/6 步已完成，等待用户确认后才进入第 3 步
- “账号非敏感配置”模板已增加 `tracking_video_ids`（当前追踪视频ID列表）；一个 ID 表示单视频，多个 ID 表示同时追踪
- 新增独立本地解析器：支持换行及中英文逗号/分号、保序去重、重复项提示、11 位 YouTube ID 格式校验和最多 10 个不同视频限制
- 空配置会明确报错并禁止自动回退到最新视频；配置模板升级会幂等补建新配置记录，不覆盖用户值
- 专项 11 项测试和全套 69 项测试通过；Ruff 通过，严格 Mypy 检查 64 个源码文件通过
- 本步骤没有访问 YouTube 或飞书、没有写业务表、没有接入第 6/8/9 个运行入口、没有安装正式环境、没有提交或推送 GitHub
- 下一次只做第 3 步：把正式服务改为批量获取、整批预检查和逐视频处理，并补齐本地测试；完成后再次停下
- 2026-09-02 手动多视频追踪改造第 1/6 步已完成，等待用户确认后才进入第 2 步
- 已完成现有单视频假设审计并建立 `手动多视频追踪设计.md`：统一使用 `tracking_video_ids`，一个 ID 表示单视频、多个 ID 表示同时追踪
- 审计确认 YouTube 客户端已有批量读取能力，三表唯一键均包含视频 ID，字段字典和动态映射也可由所有视频共用；主要改造点集中在选择入口、整批校验、逐视频执行和结果汇总
- 已确定安全边界：最多 10 个、去重保序、格式与频道归属校验、任何无效/缺失视频在写入前阻止整批任务，手动模式禁止回退到最新视频
- 超过追踪天数的视频只跳过自身，不阻塞同批有效视频；删除 ID 停止后续采集但保留历史，重新加入后不补造离线快照
- 现有全套 61 项本地测试继续通过，确认第 1 步只固化设计和边界，没有改变当前单视频运行行为
- 本步骤没有调用 YouTube 或飞书、没有写业务表、没有修改现有运行行为、没有安装正式环境、没有提交或推送 GitHub
- 下一次只做第 2 步：增加 `tracking_video_ids` 公共配置与本地解析、去重、格式校验和数量限制；完成后再次停下
- 2026-09-02 通用动态字段映射改造第 6/6 步已完成：当前源码已安装到正式运行环境，并通过小规模在线动态字段验收
- 第 9 个只读检查在线通过：三表可访问、完整同步安全状态为 true，主表识别到新增第 18 条 `VIDEO_THUMBNAIL_URL → 视频缩略图` 映射及实时文本类型
- 第 8 个只读预览在线通过：请求计划自动加入 `snippet.thumbnails.high.url`，YouTube 返回缩略图网址，缺失字段和空值均为 0，飞书写入预览正确
- 第 6 个正式同步成功：运行 ID `ae003418-872d-4ded-a729-a66c0cf201c2`，处理 1 个视频，主表/快照/同期对比写入次数为 1/1/3，共 5 次
- 飞书回读确认：视频 `okAkZVRx7ac` 的主表记录已保存 `https://i.ytimg.com/vi/okAkZVRx7ac/hqdefault.jpg`；当前三表总记录数为 1/3/9，均匹配该测试视频
- 已安装版本的全套 61 项测试通过，`pip check` 无损坏依赖；本步骤没有操作浏览器、没有安装循环调度、没有提交或推送 GitHub
- 通用字段闭环已经验证：普通 YouTube Data API 单值字段可经“API字段字典 + 启用映射 + 飞书实时类型”自动进入检查、预览和正式写入，不需要为缩略图编写专用业务代码
- 本轮 6 步工作完成；后续新增字段时先维护字段字典和映射，运行第 9/8 检查后再运行第 6 正式同步
- 2026-09-02 通用动态字段映射改造第 5/6 步已完成，等待用户确认后才进入第 6 步
- 新增配置防冲突：飞书 API 字段字典中重复的标准字段 ID、多个业务表共用同一 Table ID、启用映射缺少目标表 ID 都会在计划阶段停止
- 新增来源—目标类型矩阵：普通 Data API 的字符串、网址、数字、布尔、日期、日期时间和时长会与飞书实时字段类型预先核对；明显冲突在任何 API 请求或写入前报错
- 空值策略已验证：API 明确返回 null 时保留诊断信息，但不写空值覆盖飞书已有数据；错误但格式合法的官方路径会被报告为“响应缺失”
- 错误矩阵已覆盖：远程字典缺字段/重复、未知或停用字段、错误路径、无效源值、重复映射、缺列、未知表、缺表 ID、复杂飞书类型、实时字段结构损坏和转换失败
- 第 5 步验证：动态链路专项 33 项测试通过，全套 61 项测试通过；Ruff 和严格 Mypy（63 个源码文件）通过
- 本步骤没有访问 YouTube 或飞书、没有写业务表、没有安装或更新正式运行环境、没有提交或推送 GitHub
- 下一次只做第 6 步：把当前源码安装到正式运行环境，并执行一次小规模在线动态字段验收；完成后再次停下
- 2026-09-02 通用动态字段映射改造第 4/6 步已完成，等待用户确认后才进入第 5 步
- 飞书“API字段字典”现在会覆盖本地基础目录：已有字段可更新，新普通 Data API 单值字段可由官方路径自动推导 `part`；新 Analytics/Reporting 字段仍要求专用查询模板
- 新增动态计划编译器：把启用映射、标准字段来源、YouTube 官方路径、目标表 ID、实时字段 ID/类型和写入适配器一次性编译；重复、缺列、未知来源或不支持类型会阻止计划
- 第 9 个只读检查现在会额外输出完整动态计划及每条映射的飞书字段类型；只有基础三表检查和动态计划同时通过才允许正式同步
- 第 8 个只读预览现在会先只读加载飞书字典、映射和三表字段结构，再按动态请求计划读取 YouTube，并输出标准值、缺失/空值及飞书写入格式预览
- 第 6 个正式运行在创建 YouTube 客户端及任何业务写入前强制编译同一份动态计划；计划失败则整次任务停止
- 最新视频服务已接入通用提取和目标类型适配；本地假数据验证 `VIDEO_THUMBNAIL_URL` 无专用业务代码即可提取并转换成飞书超链接后进入三表写入路径
- 第 4 步验证：动态链路相关 8 项测试通过，全套 43 项测试通过；Ruff 和严格 Mypy（63 个源码文件）通过
- 本步骤只修改和测试源码，没有访问 YouTube 或飞书、没有真实写业务表、没有更新正式运行环境、没有提交或推送 GitHub
- 下一次只做第 5 步：补齐多种字段、错误配置、空值和类型冲突的自动化测试及必要修正；完成后再次停下
- 2026-09-02 通用动态字段映射改造第 3/6 步已完成，等待用户确认后才进入第 4 步
- 新增飞书实时表结构模型：可从 `list_fields` 响应建立字段名、字段 ID、类型编号、类型名称、属性和主字段索引
- 新增通用写入类型适配器：按实时类型支持文本、数字、单选、多选、日期时间、复选框和超链接，不根据“缩略图”等业务字段名写专用分支
- 文本字段写普通字符串；同一映射列若改为飞书超链接类型，适配器会自动生成 `{link, text}` 写入结构
- 附件、人员、关联、查找引用、公式等复杂类型当前会在写入前明确报“不支持”，不会猜测或静默写空
- 第 3 步验证：新增 6 项测试通过，全套 40 项测试通过；Ruff 和严格 Mypy（62 个源码文件）通过
- 本步骤没有把类型层接入第 6/8/9 个入口，没有访问 YouTube 或飞书、没有写业务表、没有更新正式运行环境
- 下一次只做第 4 步：把字段字典、映射、通用提取器和实时飞书类型编译成动态计划，并升级第 9、第 8、第 6 的预检查；完成后再次停下
- 2026-09-02 通用动态字段映射改造第 2/6 步已完成，等待用户确认后才进入第 3 步
- 新增 Data API 普通单值字段通用提取器：根据 API 字段字典自动校验来源、资源层级、数据类型、官方字段路径并合并所需 `part`
- 提取器可沿 `snippet.thumbnails.high.url` 等嵌套路径读取原始资源，并统一解析字符串、网址、整数、数字、布尔值、日期、日期时间和 ISO 时长
- 提取结果会分别报告“响应中缺少路径”和“API 明确返回 null”；数组、Analytics/Reporting 字段及层级不匹配会在计划阶段明确失败
- 通用性测试已覆盖缩略图网址和此前没有业务硬编码的默认语言字段；本步骤没有把新提取器接入第 6/8/9 个入口
- 第 2 步验证：新增 7 项测试通过，全套 34 项测试通过；Ruff 和严格 Mypy（61 个源码文件）通过
- 本步骤没有访问 YouTube 或飞书、没有写业务表、没有更新正式运行环境、没有提交或推送 GitHub
- 下一次只做第 3 步：建立飞书实时字段类型识别与通用类型适配器；完成后再次停下
- 2026-09-02 通用动态字段映射改造第 1/6 步已完成，等待用户确认后才进入第 2 步
- 已将中断时加入生产路径的“缩略图专用字段”和未完成的类型适配原型安全撤回；原有三表闭环能力保持不变
- 已建立 `动态字段映射框架设计.md`：普通 Data API 单值字段以“字段字典 + 启用映射 + 飞书实时字段类型”驱动，不为每个字段修改主程序
- 飞书目标字段类型以后由第 9 个只读检查实时获取，不要求在映射表中手工维护重复的类型备注
- 新框架要求第 6 个正式入口自带强制预检查；未知字段、不支持的数据形态和类型冲突必须在任何写入前失败，禁止静默跳过
- 本步骤没有调用 YouTube 或飞书、没有写业务表、没有修改调度、没有提交或推送 GitHub
- 下一次只做第 2 步：建立 YouTube Data API 普通单值字段的通用路径提取器；完成后再次停下
- 2026-09-02 第一闭环第 6/6 步已完成：中文一键入口、日常操作、字段调整、新模块扩展和 Windows/VPS 触发说明均已整理并验证
- Windows 日常手动同步固定使用 `6.运行一次_最新视频.bat`；`7.调度滴答.bat` 只供操作系统计划任务调用
- 新增 `8.只读预览_最新视频.bat` 和 `9.只读检查_三表配置.bat`，修改配置或字段后可先检查而不写业务表
- 模块 ID `latest_video_tracker` 与调度任务 ID `latest-video-tracker` 现已统一兼容，用户不再需要记忆两种英文写法
- 第 6 步验证：27 项 Pytest、Ruff、严格 Mypy（60 个源码文件）和依赖检查全部通过；安全 dry-run 确认 `external_requests_made=false`
- 本步骤没有再次读取 YouTube、没有写飞书业务表、没有安装循环调度，也没有提交或推送 GitHub
- 第一闭环代码和使用说明已经完成；下一步由用户重置已暴露的飞书 App Secret，再选择 Windows 自动调度或 VPS 迁移
- 2026-09-02 第一闭环第 5/6 步已完成：第一次真实 YouTube → 本地数据库 → 飞书三表同步成功，等待用户确认后才进入第 6 步
- 真实任务运行 ID：`d5dc5a78-549a-44d5-b1fe-737e241cb8ac`，状态 `success`
- 同步视频：`okAkZVRx7ac`；本次读取到播放量 1381、点赞数 4、评论数 5
- 实际业务写入共 5 次：主表 1 条、实时快照表 1 条、同期对比表 3 条
- 回读核对通过：该视频在主表匹配 1 条、快照表匹配 1 条、同期对比表匹配 3 条；三张表当前总记录数分别为 7、29、35
- 同期对比当前视频行样本数为 1；最近10条平均和中位数没有相同类型/时间档历史样本，样本数为 0 且指标留空，没有伪造数据
- 本步骤没有启动循环调度，只执行了一次真实任务
- 发现 CLI 名称差异：模块检查使用 `latest_video_tracker`，真实调度任务使用 `latest-video-tracker`；第 6 步将通过中文启动脚本和文档隐藏这一区别
- 下一次只做第 6 步：整理一键入口、日常操作说明、字段调整和新增模块方法；完成后再次停下
- 2026-09-02 第一闭环第 4/6 步已完成：自动化测试、静态检查和安全 dry-run 全部通过，等待用户确认后才进入第 5 步
- 自动化结果：Pytest 26 项全部通过；Ruff 全部通过；严格 Mypy 检查 60 个源码文件无问题；`pip check` 无损坏依赖
- 安全 dry-run 通过：启动配置完整，Data API 请求计划正确，`external_requests_made=false`，未访问 YouTube、未写飞书业务表
- 第 4 步修正了旧测试仍按单快照表验证的问题，并补上旧版配置表升级到三表复合映射时的兼容处理
- 正式环境已安装项目声明的开发测试工具；这些工具只保存在被 Git 忽略的 `runtime/venv` 中
- 下一次只做第 5 步：执行第一次真实、小规模三表同步并核对写入结果；完成后再次停下
- 2026-09-02 第一闭环第 3/6 步已完成：三张业务表的字段映射和协作写入逻辑已经接通，等待用户确认后才进入第 4 步
- 公共“模块字段需求与映射”现有 45 条最新视频模块映射：主表 17 条、快照表 17 条、同期对比表 11 条
- 主表采用“一个视频一行、重复运行更新同一行”；快照表采用“一个真实采集时间桶一行、不补造离线快照”
- 同期对比表为每个真实时间档生成“当前视频、最近10条平均、最近10条中位数”三类记录；无历史样本时样本数写 0、聚合指标留空
- 在线只读验收通过：三表均可访问、映射列全部存在、标准字段无重复，`safe_to_run_full_three_table_sync=true`
- 本步骤只更新了公共配置表，没有向主表、快照表或同期对比表写入业务数据
- 下一次只做第 4 步：运行自动化测试和安全 dry-run，确认不发出外部写请求；完成后再次停下
- 2026-09-02 第一闭环第 2/6 步已完成：正式运行环境、飞书和 YouTube 只读连接均已验证，等待用户确认后才进入第 3 步
- 已安装 Python 3.11.9（Python Software Foundation 有效数字签名），并重建唯一正式环境 `runtime/venv`
- 两个失效环境已可恢复地移动到 `runtime/environment_backups`，没有删除
- `yfd 0.1.0`、SQLite 连接、Alembic 迁移、OAuth 文件、Token 状态和飞书在线 Doctor 均通过
- 三表只读检查通过：主表与同期对比表仍各缺映射和写入逻辑，快照表 13 条映射可用
- 已将本机代理写为 `YFD_HTTPS_PROXY=http://127.0.0.1:10808`，并补齐 Google `httplib2` 所需的 `PySocks` 依赖
- YouTube 只读预览成功：频道“科技小V”，最新视频 ID `okAkZVRx7ac`，读取过程未写飞书或业务数据库
- 下一次只做第 3 步：补齐主表和同期对比表映射，开发主表/快照表/同期对比表三表协作写入；完成后再次停下
- 2026-09-02 第一闭环分步执行：第 1/6 步“运行环境、连接与三表结构摸底”已完成，等待用户确认后才进入第 2 步
- 第 1 步在线结果：飞书主表 45 字段、实时快照表 35 字段、同期对比表 19 字段，三表均可只读访问
- 第 1 步映射结果：配置中心现有 13 条启用映射，全部指向实时快照表；主表和同期对比表尚无映射
- 第 1 步阻塞一：`.venv` 与 `runtime/venv` 都引用已不存在的 Python 3.11 安装，正式命令当前无法启动
- 第 1 步阻塞二：YouTube OAuth 文件和刷新令牌存在，但直连 Google API 失败，`.env` 中 `YFD_HTTPS_PROXY` 为空
- 下一次只做第 2 步：重建统一 Python 运行环境，验证项目 CLI、飞书和 YouTube 只读连接；完成后再次停下
- 里程碑：M6.3 最新视频安全预览与三表同步前检查已完成
- 状态：已经能把 YouTube 真实读取与飞书/数据库写入隔离，并能在真实同步前逐表报告配置、映射、列名和实现状态
- 最近更新：2026-09-01
- Git 基线：`main` 本地提交 `2107e08`；本阶段改动尚未提交或推送
- 历史说明：当前基线提交有意删除了旧版单体脚本；旧内容仍保留在 Git 历史中，新版不直接恢复旧文件。

## 已完成

- [x] 盘点仓库、分支、远端和未提交改动
- [x] 确认仓库历史未被改写
- [x] 建立 `.gitignore`、`.env.example`、打包和依赖元数据
- [x] 建立项目架构、首次使用、Windows/VPS 部署、模块规范文档
- [x] 建立本断点文件与机器可读 `project_state.json`
- [x] 建立 Pydantic 配置、跨平台路径、公共错误与结构化日志
- [x] 建立 11 张公共业务表及 Alembic 版本表（首版迁移 `20260831_0001`）
- [x] 建立统一 `Storage`/Repository 与 SQLAlchemy 实现
- [x] 用全新临时 SQLite 运行迁移到 head，并完成频道、视频、快照、任务与锁的读写测试
- [x] 建立 YouTube OAuth 统一入口与 Data/Analytics/Reporting Gateway
- [x] 建立飞书 HTTP Gateway、配置中心解析和最近一次有效配置缓存降级
- [x] 建立版本化 API 字段能力目录（40+ 个 V1 分析字段）、需求校验和请求规划
- [x] 建立飞书记录绑定与幂等 create/update 服务
- [x] 建立公共 ArchiveService：达到阈值时安全登记唯一归档批次，V1 不自动删除数据
- [x] 建立统一任务注册表、`tick/run-once`、数据库锁、失败退避、运行记录和心跳
- [x] 验证关机/离线三小时后只运行一次真实任务，不循环补造半小时快照
- [x] 实现最新视频完整纵向切片：7 天窗口、真实快照、增量、时间桶幂等飞书同步
- [x] 达到飞书归档阈值后继续保留数据库快照，但暂停向满表写入
- [x] 建立频道历史、全部视频当前数据的 manifest/字段需求模板（未注册任务）
- [x] 建立首次配置 CLI、Doctor、数据库/字段目录/调度命令及 Windows/Linux 中文脚本
- [x] 建立飞书配置表、systemd、Docker/Compose 模板
- [x] 真实执行非交互首次配置：生成被忽略的 `.env`、迁移默认库、同步 51 个字段能力
- [x] 通过 Ruff、严格 Mypy、17 项 Pytest（65% 覆盖率，无强制阈值）；构建 wheel 并核对字段 JSON/`py.typed`
- [x] 按职责实体拆分为 `03_公共核心框架`、`04_功能模块`、`05_部署与运维`、`90_开发与测试`
- [x] 将最新视频、频道历史、全部视频拆成三个稳定英文 Python 包，并保留中文业务目录
- [x] 将虚拟环境与开发缓存收进被忽略的 `runtime/`，根目录不再展示工具缓存
- [x] 处理 Windows 中文源码路径兼容：首次配置采用普通 wheel 安装，Alembic 通过包资源加载迁移
- [x] 完成 YouTube OAuth，验证 Token 有效且具备刷新能力
- [x] 验证飞书 `bitable:app` 权限、应用发布状态和目标 Base 在线访问
- [x] 实现 `yfd feishu bootstrap-config`：同名表复用、缺失字段补建、类型冲突保护、初始记录幂等写入
- [x] 自动创建 `API字段字典`、`模块字段需求与映射`、`数据项目配置`、`账号非敏感配置`
- [x] 写入 51 条 API 字段能力、13 条最新视频字段映射、2 条项目配置和 2 条账号非敏感配置
- [x] 将四个公共 Table ID 安全回写到被 Git 忽略的 `.env`，保留 App Secret 不变
- [x] 第二次运行初始化命令：0 张新表、0 条重复配置，确认远端幂等
- [x] 读取三个现有业务表的真实字段；修正快照默认映射，13/13 个目标列在线校验通过
- [x] 新增配置中心初始化测试与飞书表/字段客户端测试；初始化阶段 19 项 Pytest 通过
- [x] 将“模块字段需求与映射”主字段升级为中文“映射名称”，保留稳定英文模块ID和标准字段ID
- [x] 为13条实时快照映射补齐模块中文名、目标表中文名、标准字段中文名、API类型、官方字段、写入方式、实现状态和备注
- [x] 为“数据项目配置”补齐中文配置名、类型、单位、允许范围、作用说明和实现状态
- [x] 将账号表的旧 `latest_video_table_id` 兼容迁移为明确的快照表配置，并补齐主表、快照表、同期对比表三个真实 Table ID
- [x] 将 API 字段字典中的 API 来源显示值改为完整的 YouTube API 中文可读名称
- [x] 保存升级前配置快照到 `runtime/backups/feishu_config_before_localization.json`，其中不含 Secret 或 OAuth Token
- [x] 第二次运行中文化命令：0 个新增字段、0 条更新、0 条新增记录，确认幂等
- [x] 当前 20 项 Pytest、Ruff、严格 Mypy 全部通过，配置中心远端加载成功
- [x] 使用飞书字段说明直接标记18个程序关键字段，不改列名、字段类型或记录值
- [x] 关键字段分组确认：API字典7个、模块映射5个、项目配置3个、账号配置3个
- [x] 建立 `yfd feishu mark-critical-fields` 幂等命令和标记前字段结构备份
- [x] 按 YouTube 官方 Data/Analytics/Reporting 文档审计 API 字段目录
- [x] 保留 V1 的51条字段，新增 V2 增量78条，飞书 API 字段字典现有129条
- [x] V2 分布：Data API 49条、Analytics API 47条、Reporting API 33条，标准字段ID无重复
- [x] 新增 `API字段字典审计说明.md`，明确业务表计算/关联字段不属于API字典
- [x] 第二次运行关键字段标记为0修改、18项保持不变；在线Doctor通过
- [x] 当前21项Pytest、Ruff、严格Mypy全部通过
- [x] 新增 `yfd modules preview latest_video_tracker`：只读获取授权频道最新视频并输出标准字段
- [x] 安全预览不构建飞书客户端、不写业务数据库；只提示本地 OAuth Token 可能自动刷新
- [x] 新增 `yfd modules validate-sync latest_video_tracker`：只读检查三张业务表 ID、可访问性、映射、实际列名和代码实现状态
- [x] 三表检查区分“当前快照链可用”和“三表完整同步可用”，不会把单表成功误报为完整闭环
- [x] 新增安全预览、三表校验和 CLI 参数测试；当前26项Pytest、Ruff、严格Mypy（60个源码文件）全部通过
- [x] 新版本已重新安装到项目 `.venv`；安装构建工具仅影响被 Git 忽略的本地环境
- [x] 将最新视频模块扩展为主表、实时快照表、同期对比表三表协作写入
- [x] 补齐主表17条、快照表17条、同期对比表11条公共字段映射
- [x] 真实 YouTube 只读预览通过，用户频道和最新视频可正常读取
- [x] 在线三表同步前检查通过，三表字段、映射和代码实现均已就绪
- [x] 第4步自动化测试通过：26项 Pytest、Ruff、严格 Mypy 和依赖检查全部成功
- [x] 调度安全 dry-run 通过：配置完整且未发起任何外部请求或业务写入
- [x] 第一次真实最新视频三表同步成功：主表1条、快照表1条、同期对比表3条
- [x] 通过飞书 API 回读并核对视频ID、标题、类型、播放/点赞/评论和对比样本信息
- [x] 建立 Windows/Linux 的真实运行、YouTube 只读预览和飞书三表只读检查中文入口
- [x] 统一兼容模块 ID 与调度任务 ID，消除用户入口中的英文命名差异
- [x] 建立“日常怎么运行、如何调整字段、如何新增模块、如何迁移 VPS”的完整操作指南
- [x] 第一闭环 6 个步骤全部完成并通过最终本地验证

## 未完成

- [ ] 按模板实现 0–24 小时 30 分钟、24–72 小时 1 小时、72 小时–7 天 3 小时，以及 14/28 天里程碑
- [ ] 调试完成后重置已在截图中暴露的飞书 App Secret，并更新 `.env`
- [ ] 在 VPS 实测 PostgreSQL、systemd 或 Docker（当前 Windows 未安装 Docker）
- [ ] 后续实现 ArchiveService 的“复制 → 数量核对 → 原表删除”显式执行器
- [x] 实现频道历史数据业务逻辑、三表同步、每日调度、批量写入和换电脑安全认领
- [ ] 后续实现全部视频当前数据业务逻辑

## 下一步

频道每日统计模块的本机部署前验收已完成。用户自行整理并上传 Git 后，另一台运行电脑先暂停统一计划任务并备份本地 `.env`、凭据和数据库，再拉取项目并双击第14个入口；该入口会自动安装当前版本并完成在线健康检查、48小时字段确认和频道三表只读验收。通过后再启用 `channel_history_enabled`、运行第12个人工同步，核对无重复后恢复统一计划任务。首次运行会按三张表各自的业务唯一键认领已有飞书记录；发现重复唯一键或绑定冲突时会在写入前停止。仍需在调试结束后重置截图中暴露过的飞书 App Secret。未经用户确认，不自动推送 Git 或在旧运行环境上提前启用频道长期任务。

## 关键决策

1. Python 要求 3.11+；顶层使用中文职责分区，内部使用稳定英文 `src` 包布局。
2. V1 使用 SQLAlchemy 2.x + Alembic；SQLite 是默认运行库，PostgreSQL 通过数据库 URL 与可选 `psycopg` 依赖接入。
3. CLI 使用 Python 标准库 `argparse`，减少部署依赖；Windows 和 Linux 脚本只包装 CLI。
4. API 字段目录保存为版本化 JSON 资源；飞书映射负责决定模块实际使用哪些字段。
5. 调度状态和锁必须在公共数据库中持久化，机器断电后按真实当前时间判断到期，不回填虚构快照。
6. 不自动提交、不推送；到关键节点只检查并报告 Git 状态。
7. 公共包名固定为 `youtube_feishu_dashboard`；业务包使用 `yfd_...`，不能再放回公共包的 `modules/` 子目录。
8. Windows 不使用包含中文源码路径的 editable install；首次配置安装普通 wheel，避免 `.pth` 系统代码页问题。
9. 飞书公共配置表以精确中文表名复用；初始化器不删除、不重命名、不自动改变已有字段类型，发现冲突即停止。
10. 业务字段映射存在飞书配置中心时，只使用已启用的远端映射；禁用记录不能被代码默认值重新启用。
11. 英文配置键和标准字段ID属于稳定程序接口，不翻译、不随飞书中文列名变化；所有用户说明放在独立中文字段。
12. 关键字段使用飞书原生“字段说明”标记，不给字段名加前后缀，避免破坏程序识别。
13. API目录按“旧版基线 + 新版增量”维护；业务表的系统生成、系统计算、飞书公式与关联字段不得伪装成官方API字段。
14. “API预览”和“业务同步”必须是两个独立入口；预览可以读取 YouTube，但不得写飞书或业务数据库。
15. 只有 `safe_to_run_full_three_table_sync=true` 才表示三张业务表完整闭环可执行；单独的快照表就绪不能替代三表验收。
16. 主表按视频 ID 幂等更新；快照表按真实采集时间桶幂等写入；同期对比表按“视频 ID + 时间档 + 对比对象”幂等更新。
17. 同期对比没有历史样本时不得伪造平均值或中位数：样本数量写 0，聚合指标留空。

## 已创建文件

- `.gitignore`
- `.gitattributes`
- `.env.example`
- `pyproject.toml`
- `requirements.txt`
- `requirements-dev.txt`
- `README.md`
- `project_state.json`
- `00_项目文档/开发进度与断点.md`
- `00_项目文档/项目架构说明.md`
- `00_项目文档/首次使用说明.md`
- `00_项目文档/Windows部署说明.md`
- `00_项目文档/VPS部署说明.md`
- `00_项目文档/模块开发规范.md`
- `00_项目文档/飞书配置中心使用说明.md`
- `00_项目文档/API字段字典审计说明.md`
- `02_配置模板/README.md`
- `03_公共核心框架/README.md`
- `03_公共核心框架/src/youtube_feishu_dashboard/core/*`
- `03_公共核心框架/src/youtube_feishu_dashboard/db/*`
- `03_公共核心框架/src/youtube_feishu_dashboard/api/youtube/*`
- `03_公共核心框架/src/youtube_feishu_dashboard/api/feishu/*`
- `03_公共核心框架/src/youtube_feishu_dashboard/catalog/*`
- `03_公共核心框架/src/youtube_feishu_dashboard/scheduler/*`
- `03_公共核心框架/src/youtube_feishu_dashboard/services/*`
- `03_公共核心框架/src/youtube_feishu_dashboard/app.py`
- `03_公共核心框架/src/youtube_feishu_dashboard/cli.py`
- `03_公共核心框架/src/youtube_feishu_dashboard/doctor.py`
- `03_公共核心框架/src/youtube_feishu_dashboard/setup_wizard.py`
- `03_公共核心框架/src/youtube_feishu_dashboard/module_contracts.py`
- `03_公共核心框架/src/youtube_feishu_dashboard/db/alembic_scripts/*`
- `alembic.ini`
- `04_功能模块/README.md`
- `04_功能模块/01_最新发布视频实时追踪/*`
- `04_功能模块/02_频道历史数据/*`
- `04_功能模块/03_全部视频当前数据/*`
- `05_部署与运维/README.md`
- `05_部署与运维/docker/*`
- `05_部署与运维/systemd/*`
- `90_开发与测试/README.md`
- `90_开发与测试/tests/*`
- `01_启动脚本/Windows/*`
- `01_启动脚本/Linux_VPS/*`
- `02_配置模板/*`

## 待验证项

- 安全预览、在线三表检查、自动化测试、dry-run 和第一次真实三表写入均已通过。
- Windows/Linux 中文入口和用户操作指南已经完成；系统级计划任务尚未安装，等待用户选择运行平台。
- 当前调度仍为静态 30 分钟/7 天，尚未按模板升级为分阶段周期及 14/28 天里程碑。
- 主表、快照表和同期对比表的 Table ID、映射、实现状态及第一次真实业务写入均已验证。
- API能力目录已扩展到129条，但内容所有者/CMS版权管理、上传处理等偏门字段仍按未来模块需求增量加入，不宣称覆盖YouTube全部管理型资源。
- 当前电脑未安装 Docker，因此只验证了 Compose YAML 与 wheel，未执行容器构建。
- PostgreSQL 接口与依赖已预留，尚未在真实 VPS 数据库运行迁移。

## 2026-09-04 字段当前可用状态调整（第2步）

- [x] API字段字典面向用户统一为三种状态：`可直接使用`、`底层已实现，待接入主程序`、`底层未实现`。
- [x] 保留内部英文状态值以及旧 `实现状态` 列、旧中文状态值的读取兼容，避免历史配置立即失效。
- [x] 第9步的字段执行策略改为逐条检查启用映射；状态不是 `可直接使用` 时给出字段级阻断原因。
- [x] 新版配置中心模板使用 `当前可用状态`；旧飞书列存在时不会重复创建新列，目录同步会继续写入旧列。
- [x] 相关27项本地测试通过，Ruff通过，严格Mypy通过；没有连接外部服务，没有执行第9、第8、第6批处理，也没有安装运行环境。
- [x] 第3步逐项审计154个标准字段，并建立强制完整覆盖的状态清单：68项可直接使用、83项底层已实现待接入、3项底层未实现。
- [x] 当前视频普通单值 Data API 字段可直接使用；数组字段 `VIDEO_TAGS`、`VIDEO_REGION_ALLOWED`、`VIDEO_REGION_BLOCKED` 明确列为底层未实现。
- [x] 当前已接入的7项 Analytics、Reach Reporting 兼容/正式字段及25项系统字段列为可直接使用；预计广告收入保留为待接入，等待后续专项修正。
- [x] 状态清单加载时会阻止重复、遗漏和未知字段，避免以后新增字典字段却忘记判定可用性。
- [x] 第3步本地全量113项测试通过，Ruff通过，严格Mypy通过；没有连接外部服务、没有安装运行环境、没有执行第9、第8、第6批处理。
- [x] 第4步实现三种 API 的统一获取时间、数据截止时间及太平洋/北京时间字段体系。
- [x] Data API 截止时间明确标为推定；Analytics/Reporting 官方太平洋日期转换为同一边界的北京显示。
- [x] 新增12个文本型时间字段；Reporting 无当前视频数据行时不再生成虚假的截止时间或获取时间。
- [x] 第4步本地全量119项测试通过，Ruff通过，严格Mypy通过；没有连接外部服务、没有安装运行环境、没有执行第9、第8、第6批处理。
- [x] 第5步接入预计广告收入并修正 Analytics 指标缓存兼容。
- [x] 收益查询明确使用美元；本机 Token 收益只读授权范围已做脱敏检查并确认存在。
- [x] 旧缓存缺少新指标时强制刷新；刷新失败时显式空值降级，避免动态写入因缺字段中断。
- [x] 第5步本地全量124项测试通过，Ruff通过，严格Mypy通过；没有连接外部服务、没有安装运行环境、没有执行第9、第8、第6批处理。
- [x] 第6步构建并安装当前源码到正式运行环境，完成脱离源码路径的本地环境验收。
- [x] 安装包包含166项字段目录、三态清单、预计广告收入与12个双时区字段；正式环境124项测试通过且依赖无损坏。
- [x] 补齐可重复构建所需的 `build` 与 `hatchling` 开发依赖；仅联网安装构建工具，未连接业务服务。
- [ ] 下一步由用户完成字段字典同步和第9→8→6在线验收。

## 阻塞问题

本地与飞书在线阶段无阻塞。Docker 验证仍需要带 Docker 的主机。调试结束前必须重置已暴露的飞书 App Secret。
